{
  "name": "solana-crowdfunding",
  "version": "1.0.0",
  "private": true,
  "description": "Solana Crowdfunding - Rapid Dash Edition",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "build:frontend": "npm run build --workspace=frontend",
    "build:contract": "cd contract && anchor build",
    "test:contract": "cd contract && anchor test"
  }
}
